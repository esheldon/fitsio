--- fitscore.c	2026-05-11 13:06:50.012650202 -0400
+++ fitscore.c	2026-05-15 09:46:07.006358505 -0400
@@ -4648,8 +4648,7 @@
 
         /* free the tile-compressed image cache, if it exists */
         if ((fptr->Fptr)->tilerow) {
-           ntilebins = 
-	    (((fptr->Fptr)->znaxis[0] - 1) / ((fptr->Fptr)->tilesize[0])) + 1;
+           ntilebins = NTILEBINS;
 
            for (ii = 0; ii < ntilebins; ii++) {
              if ((fptr->Fptr)->tiledata[ii]) {
@@ -4702,8 +4701,7 @@
         /* free the tile-compressed image cache, if it exists */
         if ((fptr->Fptr)->tilerow) {
 
-           ntilebins = 
-	    (((fptr->Fptr)->znaxis[0] - 1) / ((fptr->Fptr)->tilesize[0])) + 1;
+           ntilebins = NTILEBINS;
 
            for (ii = 0; ii < ntilebins; ii++) {
              if ((fptr->Fptr)->tiledata[ii]) {
@@ -4814,8 +4812,7 @@
      /* free the tile-compressed image cache, if it exists */
      if ((fptr->Fptr)->tilerow) {
 
-           ntilebins = 
-	    (((fptr->Fptr)->znaxis[0] - 1) / ((fptr->Fptr)->tilesize[0])) + 1;
+           ntilebins = NTILEBINS;
 
            for (ii = 0; ii < ntilebins; ii++) {
              if ((fptr->Fptr)->tiledata[ii]) {
@@ -5023,8 +5020,7 @@
      /* free the tile-compressed image cache, if it exists */
      if ((fptr->Fptr)->tilerow) {
 
-           ntilebins = 
-	    (((fptr->Fptr)->znaxis[0] - 1) / ((fptr->Fptr)->tilesize[0])) + 1;
+           ntilebins = NTILEBINS;
 
            for (ii = 0; ii < ntilebins; ii++) {
              if ((fptr->Fptr)->tiledata[ii]) {
@@ -6713,8 +6709,7 @@
           /* free the tile-compressed image cache, if it exists */
           if ((fptr->Fptr)->tilerow) {
 
-           ntilebins = 
-	    (((fptr->Fptr)->znaxis[0] - 1) / ((fptr->Fptr)->tilesize[0])) + 1;
+           ntilebins = NTILEBINS;
 
            for (ii = 0; ii < ntilebins; ii++) {
              if ((fptr->Fptr)->tiledata[ii]) {
